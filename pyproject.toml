[project]
name = "comfyui-lynnreal-omni"
version = "0.1.0"
description = "LynnReal Omni generation, references, editing and continuation in native ComfyUI"
requires-python = ">=3.10"
license = {file = "LICENSE"}

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
